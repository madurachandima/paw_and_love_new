import 'package:flutter/material.dart';

class BreedersAndSellers extends StatelessWidget {
  const BreedersAndSellers({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Breeders and Selelrs");
  }
}
