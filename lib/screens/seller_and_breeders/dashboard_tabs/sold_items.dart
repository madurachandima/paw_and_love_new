import 'package:flutter/material.dart';
 class SoldItem extends StatelessWidget {
   const SoldItem({Key? key}) : super(key: key);
 
   @override
   Widget build(BuildContext context) {
     return Container();
   }
 }
 