import 'package:flutter/material.dart';
class RequestedPurchasedItem extends StatelessWidget {
  const RequestedPurchasedItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Container(child: Text("Purchased Item"),);
  }
}
